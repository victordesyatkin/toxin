extends ../base/base


block title
    | Landing page
block main
    - let data = htmlWebpackPlugin.options.getData();
    main.landing-page(data-data=data)
        article
            block landing-page-background-wrapper
                - let src = require('../../pages/landing-page/img/landing-page.png').default;
                .landing-page__background
                    img(
                        src=src
                    )
            block landing-page-find-wrapper
                section.landing-page__find
                    block landing-page-find
                        form
                            +card
                                +picker(data.picker)
            section.landing-page__info
                block landing-page-info
                    | Лучшие номера для вашей работы, отдыха и просто вдохновения
block footer
    .landing-page__footer
        +footer(data.footer)
