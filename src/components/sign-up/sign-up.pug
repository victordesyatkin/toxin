include ../input/input
include ../radio-button/radio-button
include ../masked-text-field/masked-text-field
include ../toggle-button/toggle-button
include ../card-footer/card-footer
include ../button/button

mixin sign-up(props = {})
    - 
        let {
            title, input1, input2, radioButton1, radioButton2, birthday, email, password, submit, contribute, pay, signIn
        } = props; 
    .sign-up.js-sign-up
        if (title) 
            .sign-up__title=title
        .sign-up__first-name
            +input(input1)
        .sign-up__last-name
            +input(input2)
        .sign-up__genders
            .sign-up__gender
                +radio-button(radioButton1)
            .sign-up__gender
                +radio-button(radioButton2)
        .sign-up__birthday
            +masked-text-field(birthday)
        .sign-up__email
            +masked-text-field(email)
        .sign-up__password
            +input(password)
        .sign-up__contribute
            +toggle-button(contribute) 
        .sign-up__button-pay
            +button(pay)
        .sign-up__section-sign-in
            +card-footer(signIn)