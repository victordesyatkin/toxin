include ../title-label/title-label
include ../text-field/text-field
include ../radio-buttons/radio-buttons
include ../masked-text-field/masked-text-field
include ../toggle-button/toggle-button
include ../card-footer/card-footer
include ../button/button

mixin sign-up(props)
  if props
    -
      let {
          title, firstName, lastName, gender, birthday, email, password, contribute, pay, signIn, action
      } = props;
      let className = 'sign-up';
      let classNames = `${className} js-${className}`;
    form(class=classNames, action=action)
      if title
        div(class=`${className}__title`)
          +title-label(title)
      ul(class=`${className}__list`)
        li(class=`${className}__first-name`)
          +text-field(firstName)
        li(class=`${className}__last-name`)
          +text-field(lastName) 
        li(class=`${className}__gender`)
          +radio-buttons(gender)
        li(class=`${className}__birthday`)
          +masked-text-field(birthday)
        li(class=`${className}__email`)
          +masked-text-field(email)
        li(class=`${className}__password`) 
          +text-field(password)
        li(class=`${className}__contribute`)
          +toggle-button(contribute) 
        li(class=`${className}__button-pay`)
          +button(pay)
        li(class=`${className}__section-sign-in`)
          +card-footer(signIn)
